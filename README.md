# Data_Mining_Steam_Project

## To get started 

1) Create a venv with `python3 -m venv .venv` and activate it
2) Install packages with `pip install -r requirements.txt`
3) Read the dataset into SQLite database by running `src/get_data.py`
4) Clean the dataset by running `src/clean_data.py`
